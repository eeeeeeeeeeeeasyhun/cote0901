#include<iostream>
using namespace std;

int N = 0, M = 0;
int arr[9];
int dfs(int k)
{
	if (k == M) //끝나는부분
	{

	}
	else
	{
		
		for (int i = 1; i <= N; i++)
		{
			if()
			dfs(k + 1);
		}

	}
}

int main()
{
	cin >> N >> M;
	dfs(0);
}