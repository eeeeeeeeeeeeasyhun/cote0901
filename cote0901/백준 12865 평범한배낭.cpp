#include<iostream>
using namespace std; 
#include<set>
#include<map>
int main()
{
	int N, V, W, V = 0;
	set<int, int> s1;
	map<int, int> m1;
	cin >> N >> V; 

	for (int i = 0; i < N; i++)
	{
		int n, m;
		cin >> n >> m;
		m1.emplace(n, m);
		
	}

	for (int i = 0; i < N; i++)
	{
		
	}
		

		


}